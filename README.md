# Dog Emotion Predictor 🐶
This is an AI project that predicts a dog's emotion from an image.

## How to run
1. Install requirements: `pip install -r requirements.txt`
2. Run app: `streamlit run app.py`
